//contenido de operaciones
public class OperacionesBasicas {
    int sumasucesiva(int n) {
        int ressum = 0;
        for ( int i = 0 ; i <= n ; i++ ) {
            ressum += i;
        }
        return ressum;
    }
}